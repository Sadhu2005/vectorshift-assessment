name: CI Staging

# Runs only when code is pushed to staging (e.g. after merging feature → staging).
on:
  push:
    branches:
      - staging

jobs:
  pipeline:
    uses: ./.github/workflows/ci-reusable.yml
